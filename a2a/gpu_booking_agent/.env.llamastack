# Llama Stack / vLLM configuration (self-hosted, no external API key)
# Uses the OpenAI-compatible endpoint from Llama Stack on the cluster

ADK_MODEL=openai/gpt-oss-120b
OPENAI_API_BASE=https://llamastack-llamastack.apps.ocp.v7hjl.sandbox2288.opentlc.com/v1
OPENAI_API_KEY=not-needed

# MCP tool server URL (in-cluster service)
MCP_URL=http://gpu-booking-tool:8080/mcp

# A2A server settings
HOST=0.0.0.0
PORT=8001
